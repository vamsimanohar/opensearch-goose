# alerts-dashboard.top-firing

Use when the user wants to find the noisiest monitors (top firers) so they can fix flappy rules. Pass `dataSourceId` from `list_data_sources` and an optional `limit` (default 20).
