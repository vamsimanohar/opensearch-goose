# topology-map.drill

Use to drill into a service-map subgroup by an attribute (e.g. environment, team). Pass `dataSourceId`, `groupAttribute`, `groupValue`, and optional `dataset` (default `otel-v2-apm-service-map`).
