# topology-map.group-cards

Use when the user wants to group the topology map by an attribute (e.g. environment or team) and switch to a card grid. Pass `dataSourceId`, `groupBy`, and optional `dataset`. Each card shows the group's service count and is the entry point to drill back into the map.
