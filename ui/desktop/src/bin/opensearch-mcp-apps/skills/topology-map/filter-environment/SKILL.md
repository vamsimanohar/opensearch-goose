# topology-map.filter-environment

Use when the user wants to scope the topology to one deployment environment (matches `attributes.deployment.environment`). Pass `dataSourceId`, `environment`, and optional `dataset`.
