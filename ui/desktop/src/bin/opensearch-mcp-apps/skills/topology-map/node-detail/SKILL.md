# topology-map.node-detail

Use when the user wants the side detail panel for one topology node — health donut, requests, errors, fault rate, p50/p90/p99 latency. Pass `dataSourceId`, `serviceName`, optional `dataset`.
