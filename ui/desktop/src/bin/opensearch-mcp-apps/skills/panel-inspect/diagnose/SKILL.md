# panel-inspect.diagnose

Static panel diagnosis playbook mapping a symptom (no-results / syntax-error / timeout / wrong-data / slow) to a likely cause and fix. Invoke when the user says a dashboard panel is broken, slow, empty, or wrong.
