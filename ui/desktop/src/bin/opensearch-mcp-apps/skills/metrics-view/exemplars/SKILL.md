## metrics-view.exemplars

Static reference for the exemplar → trace pivot. Documents how an exemplar's `trace_id` / `span_id` / `service.name` labels map into the traces / spans / services apps, the OSD URL pattern, and an equivalent PPL query. No inputs.
