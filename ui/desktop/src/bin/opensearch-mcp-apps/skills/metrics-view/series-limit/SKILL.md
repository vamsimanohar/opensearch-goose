## metrics-view.series-limit

Static reference page describing the default series cap (20) on the metrics view, why it exists, and the recommended ways to raise it (aggregate first, tighten matchers, use heatmaps, override per-panel, or change the global setting). No inputs.
