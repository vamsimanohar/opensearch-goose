## metrics-view.add-to-dashboard

Static reference describing the saved-object payload for promoting a metrics view to a dashboard. Documents required fields (`type`, `attributes.title`, `attributes.visState`, search source, references) and gives an example body. Invoke when the user asks how to save a panel to a board.
