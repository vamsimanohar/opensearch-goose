# metrics-view.query

Use to author and review a PromQL query against a Prometheus-attached data source. NOTE: PromQL execution requires a Prometheus integration not yet wired in this build, so the tool renders the query without running it.
