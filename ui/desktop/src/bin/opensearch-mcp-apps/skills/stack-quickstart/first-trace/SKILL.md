# stack-quickstart.first-trace

Static 5-minute walkthrough for sending your first OpenTelemetry trace via curl, verifying it landed in `otel-v1-apm-span-*`, and opening it in Trace Analytics. Includes troubleshooting tips. Invoke when the user asks how to send a first trace, wants a curl-based hello-world, or needs to confirm trace ingestion is working.
