# dashboard-share.export-formats

Static reference for dashboard export formats: PDF, PNG, CSV, JSON. Invoke when the user asks what formats are available, what each captures, or how exports are rendered.
