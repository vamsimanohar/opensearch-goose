# dashboard-share.import-flow

Static reference for importing dashboard JSON via the saved-objects `_import` API, including conflict-resolution modes (`createNewCopies`, `overwrite`). Invoke when the user asks how to import a dashboard or restore from a backup.
