## metrics-browser.group-toggle

Static reference describing the metric-catalog grouping options (prefix / A–Z / cardinality) and layout modes (grid / rows). Invoke when the user asks how to organize the metrics catalog view.
