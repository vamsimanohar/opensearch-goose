## metrics-browser.type-badges

Static reference for the four Prometheus metric types (Counter / Gauge / Histogram / Summary) with definitions and valid PromQL operators. Invoke when the user asks what each metric type means or which operations are legal.
