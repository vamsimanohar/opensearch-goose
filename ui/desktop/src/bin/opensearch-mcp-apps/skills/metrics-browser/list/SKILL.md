# metrics-browser.list

Use to list metric names from a Prometheus data source. NOTE: this build does not yet wire a Prometheus client, so the tool returns an empty catalog with a notice. Author PromQL directly via `metrics-view` instead.
