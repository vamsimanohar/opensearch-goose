## metrics-browser.drill

Reference page for drilling from a metric into its label combinations. Optionally pass `dataSourceId` and `metricName` to echo them into the example OSD URL and PromQL/PPL. The Prometheus client is not wired in this build, so the page documents the URL pattern (`/app/metrics-explorer?metric=<name>`) and what the drill-in surfaces.
