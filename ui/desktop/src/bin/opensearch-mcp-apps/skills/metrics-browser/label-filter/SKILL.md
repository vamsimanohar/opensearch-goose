## metrics-browser.label-filter

Static reference for the four PromQL label-matcher operators (`=`, `!=`, `=~`, `!~`) with examples. Invoke when the user asks about PromQL label filtering or how to narrow series by label.
