# sizing-calculator.compute

Estimate trace storage volume per day and per month from traces-per-day, average spans-per-trace, and average span size. Invoke when the user asks how big the cluster needs to be, how much storage X traces will take, or wants a sizing back-of-envelope.
