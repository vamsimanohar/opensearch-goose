# stack-health.doc-counts

Quick "is data landing?" check across the three canonical signals (traces, service map, logs). Invoke when the user asks whether traces/logs are arriving, whether ingestion is working, or wants a fast triage on an empty UI.
