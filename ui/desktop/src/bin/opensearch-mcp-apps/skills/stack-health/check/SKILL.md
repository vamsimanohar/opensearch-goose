# stack-health.check

Summarize observability stack health: ports reference, component placeholders, and (when a `dataSourceId` is supplied) live `_cat/indices`. Invoke when the user asks if the stack is up, what ports things use, or wants a one-glance health check.
