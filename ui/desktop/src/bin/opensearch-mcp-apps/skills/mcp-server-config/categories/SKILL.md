# mcp-server-config.categories

Static reference grouping the OpenSearch MCP tools into four categories: Index ops, PPL, SQL, and Cluster info. Invoke when the user asks what tools the built-in MCP server exposes, how they're grouped, or which category covers a particular task.
