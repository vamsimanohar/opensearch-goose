# mcp-server-config.examples

Static use-case recipes for each MCP tool the built-in OpenSearch server exposes — pairs a natural-language prompt with the call an agent would make. Invoke when the user asks for examples of MCP tool use, how to phrase a question to the agent, or what a tool call looks like in practice.
