# mcp-server-config.enable

Static reference for enabling the built-in OpenSearch MCP server: opensearch.yml YAML snippet, supported transports (stdio / http), and notes on TLS and authentication. Invoke when the user asks how to turn on `plugins.mcp.enabled`, what transport to pick, or how the server inherits cluster auth.
