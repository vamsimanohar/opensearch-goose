# variables.reference

Static reference for dashboard variable types (Query, Custom, Constant, Interval, Data source, Text box). Invoke when the user asks what variable types are supported or how dashboard variables work.
