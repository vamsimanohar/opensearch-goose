# variables.preview

Static reference for the variable preview pane: live re-render, 25-row sample, regex capture highlight, manual refresh, error surfacing. Invoke when the user asks how the preview works.
