# variables.custom-variable

Static reference for the Custom (static) variable form, including the `label : value` option syntax and the 100-entry limit. Invoke when the user asks how to define a hardcoded dropdown variable.
