# variables.chained

Static reference for chained (dependent) variables: declaration order, cycles, and a cluster → namespace → service example. Invoke when the user asks how to make one variable's options depend on another.
