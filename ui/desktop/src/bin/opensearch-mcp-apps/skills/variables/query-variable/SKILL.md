# variables.query-variable

Static reference for the Query variable form: data source, query, regex, refresh trigger, and sort. Invoke when the user asks how to wire a variable that loads its options from a query.
