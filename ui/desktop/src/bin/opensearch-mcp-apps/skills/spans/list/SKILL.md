# spans.list

Use when the user wants to browse individual spans (not just trace roots), sorted by duration. Pass `dataSourceId`, optional `dataset` (defaults to `otel-v1-apm-span-*`), `status` (`error`|`ok`|`any`), `serviceName`, and `limit`.
