# index-patterns-admin.create

Use when the user asks how to create an index pattern. Reference: shows OSD UI URL, the `/api/saved_objects/index-pattern` REST endpoint, and example payloads for logs / traces / metrics.
