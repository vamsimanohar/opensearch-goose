## apm-red.genai-filters

Static reference for the GenAI filter chip dimensions: `gen_ai.agent.name`, `gen_ai.system`, `gen_ai.request.model`. Each row gives a description and a sample PPL `where` clause. Invoke when the user asks how to scope GenAI metrics to one agent / provider / model.
