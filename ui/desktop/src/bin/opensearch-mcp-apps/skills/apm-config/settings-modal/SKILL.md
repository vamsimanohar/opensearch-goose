# apm-config.settings-modal

Reference panel for the APM Settings modal. Invoke when the user asks what the APM settings page configures, where APM defaults live, or how the `APM-Config` saved object is structured.
