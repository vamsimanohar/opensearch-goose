# transformations.pipeline

Static reference for the transformation pipeline: every transform type with input / output shape, plus toggle and reorder semantics. Invoke when the user asks how transforms compose, how to disable a step, or how reordering works.
