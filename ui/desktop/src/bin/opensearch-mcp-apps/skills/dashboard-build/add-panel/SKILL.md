# dashboard-build.add-panel

Static reference for adding a panel to a dashboard: `Create new` vs `Add from library`, and the panel descriptor shape. Invoke when the user asks how to add a chart to a dashboard.
