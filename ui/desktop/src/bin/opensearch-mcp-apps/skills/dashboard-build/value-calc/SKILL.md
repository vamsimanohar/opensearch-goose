# dashboard-build.value-calc

Static reference for every value aggregation (Last/Last*/First/First*/Min/Max/Mean/Median/Variance/Count/Distinct count/Total). Invoke when the user asks what aggregations are available on a panel.
