# correlation-walkthrough.service-pull

Use to pull all signals (spans + logs) for one `service.name` in parallel. Pass `dataSourceId`, `serviceName`, optional `spansDataset` (default `otel-v1-apm-span-*`), optional `logsDataset`, optional `since` ISO.
