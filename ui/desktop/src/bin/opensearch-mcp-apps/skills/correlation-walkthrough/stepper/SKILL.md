# correlation-walkthrough.stepper

Static numbered playbook for cross-signal investigations: `metric-spike`, `error-log`, or `slow-agent`. Invoke when the user asks how to investigate a metric spike, trace an error log, or debug a slow AI agent.
