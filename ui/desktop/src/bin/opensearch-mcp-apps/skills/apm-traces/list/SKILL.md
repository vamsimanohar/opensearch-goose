# apm-traces.list

Use when the user wants an APM-shaped trace browser scoped to one service — recent root traces only. Pass `dataSourceId`, `serviceName`, and optionally `dataset` (defaults to `otel-v1-apm-span-*`).
