# core-concepts.index-patterns

Static reference table of common observability index patterns: logs-*, logs-otel-v1-*, otel-v1-apm-span-*, otel-v2-apm-service-map, .opensearch-alerting-*, and ss4o_*. Each row gives the signal, an example concrete index, and notes. Invoke when the user asks what index pattern to use, what each `*` matches, or wants the canonical name list.
