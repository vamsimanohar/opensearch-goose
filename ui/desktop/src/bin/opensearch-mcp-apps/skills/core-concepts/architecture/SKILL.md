# core-concepts.architecture

Static architecture diagram (ASCII) showing the five-stage observability pipeline: Instrumentation → Collection → Processing → Storage → Visualization. Each stage lists examples (SDKs, OTel Collector, Data Prepper, OpenSearch, OSD) and a one-line description. Invoke when the user asks how the stack fits together, what each stage does, or wants the big-picture diagram.
