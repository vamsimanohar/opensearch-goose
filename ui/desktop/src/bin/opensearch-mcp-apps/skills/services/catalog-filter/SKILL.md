# services.catalog-filter

Use this when the user wants the service catalog narrowed by environment, latency / throughput / failure-ratio buckets, or SDK language. Computes the same RED-style KPIs as `services_catalog` and applies the filters in PPL plus post-filtering for derived metrics.
