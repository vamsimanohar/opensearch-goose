# services.dependencies

Use when the user wants to browse the downstream dependencies of one service with per-dependency call count, error rate, and latency percentiles. Pass `dataSourceId`, `serviceName`, optional `dataset` and `limit`. Also covers "downstream dependency count per service".
