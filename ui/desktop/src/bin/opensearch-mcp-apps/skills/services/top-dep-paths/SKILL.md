# services.top-dep-paths

Use this when the user wants the worst (caller → callee) edges in the service map ranked by fault rate (errorCount / requestCount). Reads `otel-v2-apm-service-map` by default.
