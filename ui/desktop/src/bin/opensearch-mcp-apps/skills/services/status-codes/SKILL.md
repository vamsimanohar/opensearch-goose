# services.status-codes

Use when the user wants the per-operation HTTP status code distribution (2xx / 3xx / 4xx / 5xx) for one service. Pass `dataSourceId`, `serviceName`, optional `dataset` and `limit`.
