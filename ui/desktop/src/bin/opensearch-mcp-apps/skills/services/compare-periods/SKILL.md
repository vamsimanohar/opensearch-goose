# services.compare-periods

Use when the user wants to compare a service against itself across two time windows. Returns per-operation p99 deltas. Pass `dataSourceId`, `serviceName`, two ISO8601 windows (`window1Start`/`window1End`, `window2Start`/`window2End`), optional `dataset`.
