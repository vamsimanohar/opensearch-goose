# services.auto-refresh

Static reference: the MCP renders read-only snapshots, so refreshing a service detail panel means the host re-invokes the tool. Lists recommended interval presets (10s, 30s, 1m, 5m, 15m, 1h, off) and a client-side `setInterval` example. No inputs.
