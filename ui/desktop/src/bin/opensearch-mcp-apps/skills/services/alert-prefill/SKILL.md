# services.alert-prefill

Use this when pivoting from an alert into a service detail view. Renders an alert banner, the 5 service KPI cards (throughput / fault / error / availability / p99), and the most recent error spans for the service in the alert window.
