# services.kpi-cards

Use when the user wants the 5 service KPI cards (throughput, fault rate, error rate, availability, p99 latency) for one service. Pass `dataSourceId`, `serviceName`, and optional `dataset` (defaults to `otel-v1-apm-span-*`).
