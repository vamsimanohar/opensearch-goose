# services.correlation

Use when the user wants the correlation flyout for one operation: side-by-side spans + correlated logs (joined on traceId). Pass `dataSourceId`, `logsDataset`, `serviceName`, `operationName`, optional `spansDataset`, `traceId`, and `limit`.
