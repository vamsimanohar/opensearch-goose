# telemetry-ingestion-config.data-prepper-yaml

Static reference for the Data Prepper pipeline YAML — otel-trace-pipeline (otel_trace + otel_apm_service_map → OpenSearch) and logs-pipeline (otel_logs_source → daily logs indices). Invoke when the user asks how to configure Data Prepper, where service-map edges come from, or wants a working `data-prepper-pipelines.yaml` starter.
