# telemetry-ingestion-config.verification

Static checklist confirming end-to-end ingestion: indices receiving documents, Prometheus scrape targets healthy, and the APM UI rendering services / service map / RED panels. Invoke when the user asks how to verify the pipeline is working, why nothing shows up, or wants pre-canned curl commands to sanity-check ingestion.
