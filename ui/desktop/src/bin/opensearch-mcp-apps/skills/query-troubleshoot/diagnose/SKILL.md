# query-troubleshoot.diagnose

Static PPL / PromQL diagnosis playbook mapping common query symptoms to fixes. Invoke when the user's PPL or PromQL query returns no results, errors, or times out — pass `kind` to filter to one language.
